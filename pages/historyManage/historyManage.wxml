<view class="page">
    <view class="page__bd">
        <view class="weui-tab">
            <view class="weui-navbar">
                <block wx:for="{{tabs}}" wx:key="*this">
                    <view id="{{index}}" class="weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}" bindtap="tabClick">
                        <view class="weui-navbar__title">{{item}}</view>
                    </view>
                </block>
                <view class="weui-navbar__slider" style="left: {{sliderLeft}}px; transform: translateX({{sliderOffset}}px); -webkit-transform: translateX({{sliderOffset}}px);"></view>
            </view>
            <view class="weui-tab__panel">
                <view class="weui-tab__content" hidden="{{activeIndex != 0}}">
                  {{message}}
                  <!-- <view class="time-form_bg">
                    <view class="time-form_content">
                      <view class="time-form-table time-form-left">
                        <view class="time-form-text">开始时间</view>
                        <view class="weui-cell__bd">
                          <picker mode="date" value="{{startDate}}" bindchange="bindDateStart">
                              <view class="weui-input">{{startDate}}</view>
                          </picker>
                        </view>
                      </view>
                      <view class="time-form-table time-form-center">
                        <view class="time-form-text">—</view>
                      </view>
                      <view class="time-form-table time-form-right">
                        <view class="time-form-text">结束时间</view>
                        <view class="weui-cell__bd">
                          <picker mode="date" value="{{endDate}}" bindchange="bindDateEnd">
                              <view class="weui-input">{{endDate}}</view>
                          </picker>
                        </view>
                      </view>
                    </view>
                    <button class="weui-btn" type="primary" bindtap="historyMedSch">查询</button>
                  </view> -->
                  
                  <block class="weui-panel__bd" wx:for="{{drugs}}" data-item="item" wx:for-index="idx" wx:key="*this">
                    <view class="weui-media-box weui-media-box_text">
                        <view class="weui-media-box__desc">药品名称：{{item.drug_name}}</view>
                        <view class="weui-media-box__desc">服用剂量：{{item.dose}}</view>
                        <view class="weui-media-box__desc">服用方法：{{item.methods}}</view>
                        <view class="weui-media-box__desc">具体时间：{{item.spe_time}}</view>
                    </view>
                  </block>
                </view>
                <view class="weui-tab__content" hidden="{{activeIndex != 1}}">{{message}}
                  <block class="weui-panel__bd" wx:for="{{warning}}" data-item="item" wx:for-index="idx" wx:key="*this">
                    <view class="weui-media-box weui-media-box_text">
                        <view class="weui-media-box__desc">药品名称：{{item.drug_name}}</view>
                        <view class="weui-media-box__desc">具体时间：{{item.time}}</view>
                        <view class="weui-media-box__desc">服药错误类型：{{item.kinds}}</view>
                    </view>
                  </block>
                </view>
                <view class="weui-tab__content" hidden="{{activeIndex != 2}}">{{message}}
                  <block class="weui-panel__bd" wx:for="{{reminds}}" data-item="item" wx:for-index="idx" wx:key="*this">
                    <view class="weui-media-box weui-media-box_text">
                        <view class="weui-media-box__desc">药品名称：{{item.drug_name}}</view>
                        <view class="weui-media-box__desc">提醒次数：{{item.count}}</view>
                    </view>
                  </block>
                </view>
            </view>
        </view>
    </view>
</view>